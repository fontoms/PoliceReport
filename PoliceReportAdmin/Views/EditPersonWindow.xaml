<Window x:Class="PoliceReportAdmin.EditPersonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modifier Personne" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Nom:"/>
        <TextBox Grid.Row="0" Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

        <Label Grid.Row="1" Content="Prénom:"/>
        <TextBox Grid.Row="1" Text="{Binding Prenom, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

        <Label Grid.Row="2" Content="Âge:"/>
        <TextBox Grid.Row="2" Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

        <Button Grid.Row="4" Content="Enregistrer" Click="Enregistrer_Click" HorizontalAlignment="Center" Margin="5"/>
    </Grid>
</Window>
